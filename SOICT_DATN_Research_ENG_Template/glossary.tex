%\makeglossaries
\makenoidxglossaries

% Danh mục thuật ngữ và từ viết tắt
\newglossaryentry{llm}{
    type=\acronymtype,
    name={LLM},
    description={Large language model},
    first={large language model (LLM)}
}

\newglossaryentry{bos}{
    type=\acronymtype,
    name={BOS},
    description={Beginning-of-sequence token},
    first={beginning-of-sequence (BOS) token}
}

\newglossaryentry{clt}{
    type=\acronymtype,
    name={CLT},
    description={Cross-layer transcoder; a sparse replacement module whose features can write to multiple later layers},
    first={cross-layer transcoder (CLT)}
}

\newglossaryentry{dag}{
    type=\acronymtype,
    name={DAG},
    description={Directed acyclic graph; the graph structure required for both attribution graphs and final summary graphs},
    first={directed acyclic graph (DAG)}
}

\newglossaryentry{ilp}{
    type=\acronymtype,
    name={ILP},
    description={Integer linear program; the exact clustering formulation used to assign feature nodes to supernodes},
    first={integer linear program (ILP)}
}

\newglossaryentry{mlp}{
    type=\acronymtype,
    name={MLP},
    description={Multilayer perceptron; the transformer sublayer replaced by transcoders in the circuit-tracing backbone},
    first={multilayer perceptron (MLP)}
}

\newglossaryentry{sae}{
    type=\acronymtype,
    name={SAE},
    description={Sparse autoencoder; a dictionary-learning model that recovers sparse feature directions from activations},
    first={sparse autoencoder (SAE)}
}

\newglossaryentry{shap}{
    type=\acronymtype,
    name={SHAP},
    description={SHapley Additive exPlanations; the input-token attribution method used as a relevance proxy},
    first={SHapley Additive exPlanations (SHAP)}
}