\documentclass[../main.tex]{subfiles}
\begin{document}

% \textcolor{red}{Chú ý: Chương này là không bắt buộc. Nếu nghiên cứu chỉ có đánh giá thực nghiệm mà không có phần thích lý thuyết thì sinh viên không cần trình bày chương này.} 

\section{Theretical Analysis of influence}
It would be nice if we can prove that influence becomes less predictive of nodes importance as error builds up through the layers. That is, embedding nodes and feature nodes of earlier layers influence are less accurate than feature nodes of close to final layers.
% Nếu đồ án có phần phân tích lý thuyết thì sinh viên trình bày ở chương này. 

% Kết quả lý thuyết có thể là phần tính toán về độ phức tạp tính toán của thuật toán hoặc chứng minh về tỉ số hiệu năng, \ldots

% Nếu đồ án không có phần phân tích lý thuyết thì sinh viên không cần viết chương này. 

\section{Relation of supernode abstraction to causal graph summarization}
Causal DAG is a tool used in causal inference, it is not weighted. Our supernode abstraction stage is the equivilant of causal DAG summarization for weighted graphs.

\end{document}